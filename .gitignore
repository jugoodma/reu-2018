# these files are temp files generated by emacs
*~
*#

# these files are general data files that are usually too big to upload
# we don't want to clutter the repo anyway
*.csv
*.json
*.txt
*.zip
*.mp4
*.jpg
*.cache

# these are misc files that should be ignored
__pycache__/
visuals/spatial/vis-data.html
visuals/spatial/output-data.html

# these are files that we WANT in the repo
!audio-video-dataset.json
